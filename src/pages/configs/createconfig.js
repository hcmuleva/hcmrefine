import React from 'react';

const ConfigCreateWithProject = ({item}) => {
    console.log("Conf with pro",item)
    return (
        <div>
            <h1>ConfigCreateWithProject</h1>
        </div>
    );
};

export default ConfigCreateWithProject;